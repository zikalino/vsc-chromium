- item-types:
    - browser-target
  fields:
    type:
      path: "$.result.targetInfo.type"
    url:
      path: "$.result.targetInfo.url"
- item-types:
    - browser-process
  fields:
    repo_tag:
      path: $.[0].RepoTags[0]
